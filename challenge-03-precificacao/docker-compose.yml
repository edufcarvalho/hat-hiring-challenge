version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=sqlite:///./data/challenge.db
      - MARGEM_MINIMA_PCT=40.0
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
